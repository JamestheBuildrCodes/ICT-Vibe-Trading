# CRT

This directory documents the canonical Candle Range Theory implementation used by this repository.

The initial canonical strategy is **CRT-3C-v1.0**. It is deliberately explicit so contributors and AI coding agents cannot silently substitute a different CRT interpretation.

See `original-three-candle-model.md` for the model and `execution-model.md` for execution rules.
